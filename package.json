{
  "scripts": {
    "pretest": "rm -rf artifacts && mkdir -p artifacts",
    "test": "node ./index.js",
    "posttest": "ls -lhta artifacts"
  },
  "dependencies": {
    "puppeteer": "^1.10.0"
  }
}
